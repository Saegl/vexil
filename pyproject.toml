[project]
name = "vexil"
version = "0.1.0"
description = "Programming language"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llvmlite>=0.43.0",
]

[dependency-groups]
dev = [
    "mypy>=1.15.0",
    "pytest>=8.3.0",
]
